export enum Routes {
  LOGIN = "/login",
  REGISTER = "/register",
  OVERVIEW = "/overview",
  LIBRARY = "/library",
  TEAMS = "/teams",
  USERS = "/teams/users",
  ORGANIZATIONS = "/teams/organizations",
  SETTINGS = "/settings",
  PUBLIC = "/public"
}
export enum PopupTypes {}
export enum PopupModes {
  EDIT = "edit",
  ADD = "add",
  DELETE = "delete"
}
export enum ThemeMode {
  DARK = "dark",
  LIGHT = "light"
}
export enum Roles {
  OWNER = "owner",
  ADMIN = "admin",
  MEMBER = "member"
}
