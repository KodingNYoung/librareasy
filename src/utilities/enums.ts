export enum Routes {
  LOGIN = "/login",
  REGISTER = "/register",
  OVERVIEW = "/overview",
  LIBRARY = "/library",
  TEAMS = "/teams",
  SETTINGS = "/settings",
  PUBLIC = "/public"
}
export enum PopupTypes {}
export enum ThemeMode {
  DARK = "dark",
  LIGHT = "light"
}
